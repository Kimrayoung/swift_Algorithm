//
//  main.swift
//  Algorithm
//
//  Created by 김라영 on 2023/05/26.
//

/*
import Foundation
//덱_백준
let num = Int(readLine()!)!
var deque: [Int] = []

for _ in 0..<num {
    let command = readLine()!.components(separatedBy: " ")
    
    commandPrint(command)
}

func commandPrint(_ commandArr: [String]) {
    let command = commandArr[0]
    
    switch command {
    case "push_back":
        deque.append(Int(commandArr[1])!)
    case "push_front":
        deque.insert(Int(commandArr[1])!, at: 0)
    case "pop_front":
        print(deque.isEmpty ? -1 : deque.removeFirst())
    case "pop_back":
        print(deque.isEmpty ? -1 : deque.removeLast())
    case "size":
        print(deque.count)
    case "empty":
        print(deque.isEmpty ? 1 : 0)
    case "front":
        print(deque.isEmpty ? -1 : deque.first!)
    case "back":
        print(deque.isEmpty ? -1 : deque.last!)
    default:
        return
    }
}
*/
